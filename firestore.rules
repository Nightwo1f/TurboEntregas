rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // O app mobile acessa os dados pela API do backend.
      // O backend usa Firebase Admin SDK, que nao depende destas rules.
      allow read, write: if false;
    }
  }
}
